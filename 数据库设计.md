# 云数据库集合设计

本文档定义了"瓷砖修复服务"小程序的云数据库集合结构。

## 集合列表

1. **users** - 用户表
2. **cases** - 案例表
3. **appointments** - 预约表
4. **settings** - 系统设置表
5. **admins** - 管理员表
6. **statistics** - 统计表（可选）

---

## 1. users 集合（用户表）

存储所有授权登录的用户信息。

### 字段结构

```javascript
{
  _id: String,              // 系统自动生成
  _openid: String,          // 微信 openid（系统自动注入）
  nickName: String,         // 用户昵称
  avatarUrl: String,        // 用户头像 URL
  gender: Number,           // 性别：0-未知 1-男 2-女
  phone: String,            // 手机号（如有）
  registerTime: Date,       // 注册时间
  lastLoginTime: Date,      // 最后登录时间
  bookingCount: Number,     // 预约次数（冗余字段，便于统计）
  createTime: Date,         // 记录创建时间
  updateTime: Date          // 记录更新时间
}
```

### 权限设置

- **所有用户可读，仅创建者可读写**

---

## 2. cases 集合（案例表）

存储所有修复案例，由管理员添加。

### 字段结构

```javascript
{
  _id: String,              // 系统自动生成
  title: String,            // 案例标题
  type: String,             // 修复类型：hollow/crack/scratch/falloff/grout
  typeName: String,         // 类型名称（冗余，便于展示）
  coverImage: String,       // 封面图 URL
  images: Array<String>,    // 图片集 URL 数组
  video: String,            // 视频 URL（可选）
  description: String,      // 文字描述
  beforeImage: String,      // 修复前图片（可选）
  afterImage: String,       // 修复后图片（可选）
  viewCount: Number,        // 浏览量
  sortOrder: Number,        // 排序权重（数字越小越靠前）
  status: String,           // 状态：published-已发布/draft-草稿
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

### 权限设置

- **所有用户可读，仅管理员可写**

---

## 3. appointments 集合（预约表）

存储所有用户提交的预约记录。

### 字段结构

```javascript
{
  _id: String,              // 系统自动生成
  _openid: String,          // 提交者的 openid
  name: String,             // 姓名
  phone: String,            // 联系电话
  address: String,          // 服务地址
  date: String,             // 上门日期（YYYY-MM-DD 格式）
  period: String,           // 上门时段：上午/下午
  description: String,      // 问题描述（可选）
  status: String,           // 状态：pending/contacted/completed/cancelled
  adminRemark: String,      // 管理员备注（可选）
  contactTime: Date,        // 联系时间
  completeTime: Date,       // 完成时间
  cancelTime: Date,         // 取消时间
  createTime: Date,         // 提交时间
  updateTime: Date          // 更新时间
}
```

### 状态流转

```
pending（待处理）
  ↓ 管理员联系
contacted（已联系）
  ↓ 服务完成
completed（已完成）

pending（待处理）
  ↓ 用户/管理员取消
cancelled（已取消）
```

### 权限设置

- **仅创建者可读写，管理员可读全部**

---

## 4. settings 集合（系统设置表）

存储系统配置信息，通常只有一条记录。

### 字段结构

```javascript
{
  _id: String,              // 系统自动生成
  banners: Array<String>,   // 轮播图 URL 数组
  companyIntro: String,     // 公司简介
  phone: String,            // 联系电话
  qrcodeUrl: String,        // 工作微信二维码图片 URL
  workTime: String,         // 工作时间
  serviceArea: String,      // 服务区域
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

### 权限设置

- **仅管理员可读写**

---

## 5. admins 集合（管理员表）

存储管理员账号信息，用于权限验证。

### 字段结构

```javascript
{
  _id: String,              // 系统自动生成
  _openid: String,          // 管理员的 openid
  name: String,             // 管理员姓名
  phone: String,            // 管理员手机号
  wechat: String,           // 管理员微信号
  role: String,             // 角色：super_admin/admin
  status: String,           // 状态：active-启用/disabled-禁用
  remark: String,           // 备注
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

### 权限设置

- **仅管理员可读（用于验证身份）**

---

## 6. statistics 集合（统计表，可选）

存储访问统计数据，用于图表展示。

### 字段结构

```javascript
{
  _id: String,              // 系统自动生成
  date: String,             // 日期（YYYY-MM-DD）
  type: String,             // 统计类型：visit/booking/case_view
  count: Number,            // 数量
  detail: Object,           // 详细数据（可选）
  createTime: Date,         // 创建时间
  updateTime: Date          // 更新时间
}
```

### 权限设置

- **仅管理员可读写**

---

## 初始化建议

### 1. 创建集合

在微信云开发控制台依次创建以上 6 个集合。

### 2. 设置权限

根据每个集合的权限设置，在云开发控制台配置相应的读写权限。

### 3. 初始化管理员

在 `admins` 集合中手动添加一条记录：

```javascript
{
  _openid: "管理员的openid",  // 通过 wx.cloud.callFunction 获取
  name: "管理员",
  role: "super_admin",
  status: "active",
  createTime: new Date()
}
```

### 4. 初始化设置

在 `settings` 集合中添加一条默认记录：

```javascript
{
  banners: [],
  companyIntro: "专业瓷砖修复服务，十年行业经验，为您提供优质的上门服务。",
  phone: "138-0000-0000",
  qrcodeUrl: "",
  workTime: "周一至周六 9:00-18:00",
  serviceArea: "全市范围上门服务",
  createTime: new Date(),
  updateTime: new Date()
}
```

---

## 索引建议

为提高查询性能，建议创建以下索引：

### users 集合
- `_openid`（唯一索引）

### cases 集合
- `type`
- `status`
- `createTime`（降序）

### appointments 集合
- `_openid`
- `status`
- `createTime`（降序）

### settings 集合
- 无需额外索引（只有一条记录）

### admins 集合
- `_openid`（唯一索引）

### statistics 集合
- `date`
- `type`

---

## 云函数建议

### 1. 获取用户 openid

创建云函数 `getOpenId`，用于获取用户的 openid。

### 2. 检查管理员权限

创建云函数 `checkAdmin`，验证当前用户是否为管理员。

### 3. 提交预约

创建云函数 `submitAppointment`，处理预约提交逻辑（发送订阅消息通知管理员等）。

### 4. 更新预约状态

创建云函数 `updateAppointmentStatus`，更新预约状态并发送通知。

---

## 注意事项

1. 所有时间字段建议使用 `db.serverDate()` 确保服务器时间一致
2. 图片/视频 URL 通过云存储上传后获取
3. 敏感字段（如手机号）在前端展示时需注意脱敏
4. 定期备份数据库，防止数据丢失
5. 合理设置云数据库配额，避免超出免费额度
